{
  "name": "app",
  "private": true,
  "scripts": {
    "format": "prettier . --check",
    "format:fix": "prettier . --write",
    "test:all": "pnpm -r --parallel test",
    "lint:all": "pnpm -r --parallel lint",
    "check-types:all": "pnpm -r --parallel check-types"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "5.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
